---
title: Tidy executor
description: Runs go mod tidy to sync go.mod with the project source code.
---

import SchemaOptionsTable from '@naxodev/docs-theme/components/SchemaOptionsTable.astro';
import schema from '@naxodev/gonx/src/executors/tidy/schema.json';

The tidy executor runs `go mod tidy` to ensure the go.mod file matches the project source code.

## Usage

```bash
nx tidy my-go-project
```

## Options

<SchemaOptionsTable schema={schema} />

## Inferred target

Inferred for all Go projects (applications and libraries):

```json
{
  "executor": "@naxodev/gonx:tidy",
  "cache": true,
  "inputs": ["goSource", "^goSource"]
}
```

See [caching and inputs](/guides/generators/add-go-to-existing-workspace#caching-and-inputs) for what `goSource` and `^goSource` hash.

## Next steps

- [Lint executor](/guides/executors/lint)
- [Plugin options](/guides/generators/options)
