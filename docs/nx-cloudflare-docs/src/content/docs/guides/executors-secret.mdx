---
title: secret executor
description: Option reference for the secret executor and its put, bulk, list, and delete configurations, plus the versioned variant.
---

import SchemaOptionsTable from '@naxodev/docs-theme/components/SchemaOptionsTable.astro';
import schema from '@naxodev/nx-cloudflare/src/executors/secret/schema.json';

The `@naxodev/nx-cloudflare:secret` executor runs `wrangler secret <command>` for a Worker. Secret values are never passed as command arguments. The plugin infers a `secret` target for **every** Worker, exposing four Nx configurations — `put`, `bulk`, `list`, and `delete` — that set `command` for you. See [Manage Worker secrets](/guides/secrets) for a task-oriented walkthrough.

## Options

<SchemaOptionsTable schema={schema} />

`command` is set by the inferred target's configuration. `versioned` is set by the `version-secret` target's configuration (see below) — you don't pass either yourself.

## Configurations

### put

Runs `wrangler secret put <name>`. Prompts interactively for the value; requires `--name`.

```bash
bunx nx run my-worker:secret:put --name=API_KEY
```

### bulk

Runs `wrangler secret bulk <file>`. Uploads multiple secrets from a JSON file; requires `--file`. Do not commit that file.

```bash
bunx nx run my-worker:secret:bulk --file=secrets.json
```

### list

Runs `wrangler secret list`. Lists the names of all secrets bound to the Worker — values are never exposed.

```bash
bunx nx run my-worker:secret:list
```

### delete

Runs `wrangler secret delete <name>`. Requires `--name`.

```bash
bunx nx run my-worker:secret:delete --name=API_KEY
```

## The versioned variant

The plugin also infers a `version-secret` target alongside `secret`, running the same executor with `versioned: true` baked in by its configuration. Where `secret` acts on the **currently deployed** Worker, `version-secret` stages the same `put`/`bulk`/`list`/`delete` operations on a **new version** (`wrangler versions secret <command>`) without shifting traffic — pair it with `version-deploy` for [gradual deployments](https://developers.cloudflare.com/workers/configuration/versions-and-deployments/).

```bash
bunx nx run my-worker:version-secret:put --name=API_KEY
bunx nx run my-worker:version-deploy
```

## Notes

- Every configuration accepts `--env <environment>` for a [Wrangler environment](https://developers.cloudflare.com/workers/wrangler/environments/).
- Secrets are not declared in `wrangler.jsonc`, so the `secret` and `version-secret` targets are inferred for every Worker regardless of config format (jsonc/json/toml).
- Arbitrary `-- <wrangler flag>` passthrough does **not** apply to this executor — only the typed options above are accepted.

## Next steps

- [Manage Worker secrets](/guides/secrets) — step-by-step guide
- [Inferred targets](/inferred-targets) — the full target reference
- [application generator](/guides/generators-application) — scaffold a Worker
