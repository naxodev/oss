{
  "$schema": "https://json-schema.org/schema",
  "version": 2,
  "title": "Tidy executor",
  "description": "Ensures go.mod file matches a project's source code",
  "type": "object",
  "properties": {
    "verbose": {
      "type": "boolean",
      "description": "Enable verbose output",
      "default": false
    }
  },
  "required": []
}
