package cmd

import (
	"fmt"
	"os"

	tea "github.com/charmbracelet/bubbletea"
	"<%= moduleName %>/internal/ui"
)

func Execute() {
	m := ui.NewModel()
	p := tea.NewProgram(m)
	
	if _, err := p.Run(); err != nil {
		fmt.Printf("Error: %v\n", err)
		os.Exit(1)
	}
}
