package cmd

import (
	"fmt"
	"os"
	"<%= moduleName %>/internal/models"

	tea "github.com/charmbracelet/bubbletea"
)

func Execute() {
	m := models.NewModel()
	p := tea.NewProgram(m)
	
	if _, err := p.Run(); err != nil {
		fmt.Printf("Error: %v\n", err)
		os.Exit(1)
	}
}
