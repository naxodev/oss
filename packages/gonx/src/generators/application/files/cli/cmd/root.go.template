package cmd

import (
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

var rootCmd = &cobra.Command{
	Use:   "<%= projectName %>",
	Short: "<%= projectName %> is a CLI application",
	Run: func(cmd *cobra.Command, args []string) {
		fmt.Println("Hello from <%= projectName %>!")
	},
}

func Execute() {
	if err := rootCmd.Execute(); err != nil {
		os.Exit(1)
	}
}
